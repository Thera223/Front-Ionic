import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ModifermotdepassPage } from './modifermotdepass.page';

describe('ModifermotdepassPage', () => {
  let component: ModifermotdepassPage;
  let fixture: ComponentFixture<ModifermotdepassPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ModifermotdepassPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
